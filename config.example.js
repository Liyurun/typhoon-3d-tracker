/* 本地/部署配置示例。
 * 使用方式：复制为 config.js（已在 .gitignore 中忽略），填入你自己的 Cesium ion Token。
 * Token 在浏览器中始终可见，因此请在 Cesium ion 控制台把 Allowed URLs 限制为你的域名。
 * 不配置时地球正常显示，仅地形降级为平滑球面。 */
window.CESIUM_ION_TOKEN = "";
